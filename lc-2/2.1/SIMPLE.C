#include<stdio.h>
#include<conio.h>
#define p prinft
#define ch clrscr
#define ge getch
main()
{
   float p, r,n ,si;
   ch();
   p("anter a value:");
   scanf("%f",&p);
   p("anter a value:");
   scanf("%f",&r);
   p("anter a value:");
   scanf("%f",&t);
   si=p*r*n/100;
   p("simple interst is:%f",si);
   ge();
}