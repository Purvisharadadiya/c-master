#include<stdio.h>
#include<conio.h>
main()
{
      int startyear,endyear;
      clrscr();
      printf("enter the  fist number (startyear):");
      scanf("%d",&startyear);
      printf("enter the  second number (endyear):");
      scanf("%d",&startyear);

      while(staryear<=endyear)
      {
	if ((startyear %4==0&& startyeae%100!=0)(staryeae%400=0))
	printf("%d\n",startyear);
	startyeaer++;
      }
      getch();
}